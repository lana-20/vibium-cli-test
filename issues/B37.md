# B37 — `vibium find` mints a `@ref` for a CSS selector that matched nothing

**vibium v26.5.31 · macOS darwin 25.5.0**

> # ⛔ DO NOT FILE — fixed on `main` before this draft existed
>
> **Fixed 2026-08-03 by [#206](https://github.com/VibiumDev/vibium/issues/206)**
> (closed/completed), commit *"Make `is` exit-honest and `find --timeout` real"*
> (PR #280) — one day after `hugs` named the defect while closing #212.
>
> The fix was a **side effect** of making `--timeout` real on the CSS path: adding
> polling routed a miss through the error branch. The source on `main` says so in the
> past tense, at `internal/agent/handlers.go`:
>
> ```go
> // Poll like the semantic branch does, so --timeout means something here and
> // a CSS find waits for an element that has not rendered yet (#206). The
> // single non-polling call also returned nil for a miss and still minted a
> // @e1 ref, so `find` reported "@e1 <nil>" with exit 0.
> labelResult, err := pollCallFunction(h, labelScript, []interface{}{selector}, timeout)
> if err != nil {
>     return nil, fmt.Errorf("element not found: %s (timeout %s)", selector, timeout)
> }
> ```
>
> **This draft claimed "unfiled and unfixed — no commit since touches it (checked
> across all 198 commits)." That was wrong.** The fix commit was in the scan; the
> keyword grep (`browserFind`, `mint`, `nil`, `guard`) simply did not match a commit
> titled after `is` and `--timeout`. **Fourth false negative from keyword-scanning
> commit messages in one session** — see the methodology note at the foot of this file.
>
> It still reproduces on installed v26.5.31 only because nothing is published
> (installed = npm `latest` = v26.5.31, 2026-06-01). Everything measured below is
> accurate *for that build* and is kept as the record of what the bug was.

**Filed as:** _never filed — fixed upstream first._

## Summary

`vibium find "<css>"` returns a usable-looking element ref and exits 0 even when the
selector matches nothing. The output embeds a raw Go `<nil>`:

```sh
vibium go https://example.com
vibium find "#nope-zzz"
# → @e1 <nil>
# exit 0
```

With `--json` it is reported as an outright success:

```sh
vibium find "#nope-zzz" --json
# → {"ok":true,"result":"@e1 <nil>"}
# exit 0
```

The failure surfaces later, at the point of use, blaming the wrong command:

```sh
vibium click "@e1"
# → Error: failed to click: timeout after 0s: element not found
# exit 1
```

**Only the CSS-selector path is affected.** Every semantic locator, `--all`, and the
sibling `count` command all handle a miss correctly — so this is an inconsistency inside
one subcommand, not a missing capability.

## Where this came from

This defect was **named by a maintainer**, not by us. Closing
[#212](https://github.com/VibiumDev/vibium/issues/212) on 2026-08-02 (a report whose own
premise did not hold), `hugs` wrote:

> There is one real defect in the neighbourhood, though it is not what was reported:
> `browserFind` writes a @ref without checking that the lookup returned anything
> (`handlers.go:1020-1027`), so a miss can still mint a ref. Worth a 3-line guard.

No issue was opened for it — but it **was fixed the next day**, via #206's unrelated
`--timeout` work (see the banner). The original draft asserted "no commit since touches
it" on the strength of a keyword grep; that was wrong. This write-up otherwise supplies
the repro, the blast radius, and the two symptoms not mentioned in that sentence.

## The two symptoms beyond the original description

1. **A raw Go `<nil>` reaches user-facing output.** Not a formatting nit — it is the
   only signal that anything went wrong, and it appears *inside* an otherwise
   well-formed ref line.
2. **`--json` reports `"ok":true`.** Any machine consumer — a script, an agent, a CI
   step — reads a failed lookup as a success. This is the more serious of the two,
   because the plain-text `<nil>` is at least visible to a human.

## The correct behaviour already exists, in the same subcommand

Measured on v26.5.31, `https://example.com`, all selectors guaranteed to match nothing:

| command | output | exit | correct? |
|---|---|---|---|
| `find "#nope-zzz"` | `@e1 <nil>` | 0 | ✗ **mints a ref** |
| `find "#nope-zzz" --json` | `{"ok":true,"result":"@e1 <nil>"}` | 0 | ✗ **reports success** |
| `find "#nope-zzz" --all` | `No elements found` | 0 | ✓ |
| `find text "NoSuchTextZZZ"` | `Error: element not found: text=NoSuchTextZZZ (timeout 30s)` | 1 | ✓ |
| `find label "NoSuchLabelZZZ"` | `Error: element not found: label=NoSuchLabelZZZ (timeout 30s)` | 1 | ✓ |
| `find placeholder "NoSuchPlaceholderZZZ"` | `Error: element not found: placeholder=… (timeout 30s)` | 1 | ✓ |
| `count "#nope-zzz"` | `0` | 0 | ✓ |

Control, same session — a selector that *does* match:

```sh
vibium find "h1"
# → @e1 [h1] "Example Domain"
# exit 0
```

**All six semantic locators verified** (upgraded 2026-08-07 — the first draft could only
claim three):

| command | output | exit |
|---|---|---|
| `find testid "nosuch-testid-zzz"` | `Error: element not found: testid=… (timeout 30s)` | 1 ✓ |
| `find xpath "//div[@id='nope-zzz']"` | `Error: element not found: xpath=… (timeout 30s)` | 1 ✓ |
| `find role button --name "NoSuchZZZ"` | `Error: element not found: role=button, text=NoSuchZZZ (timeout 30s)` | 1 ✓ |

So **every** locator form except the CSS path handled a miss correctly on v26.5.31.

## The minted ref overwrites a previously valid one

The bogus ref is not appended — it takes the `@e1` slot. A sequence that finds a real
element and then misses silently loses the good ref:

```sh
vibium find "h1"          # → @e1 [h1] "Example Domain"
vibium find "#nope-zzz"   # → @e1 <nil>            ← @e1 now points at nothing
vibium click "@e1"        # → Error: ... element not found
```

## Impact

The exit-0 miss defeats the standard defensive pattern. A script that checks `find`'s
exit code before using the ref passes the check and fails at the *next* command, with an
error naming `click` rather than the selector that was actually wrong:

```sh
if vibium find "$SEL"; then
  vibium click "@e1"      # ← fails here, pointing at the wrong step
fi
```

For agent use the `--json` case is worse: `{"ok":true}` is exactly the field an agent
keys on to decide the step succeeded.

Severity **Medium**, priority **P2** — not data-destructive and there is a workaround,
but it converts a clear failure into a misattributed one, and the `--json` path is
silently wrong to machine consumers.

## Workaround

Use `count` as a guard, or `find --all`, both of which report a miss correctly:

```sh
[ "$(vibium count "$SEL")" -gt 0 ] && vibium click "$SEL"
```

## Suggested fix

The guard `hugs` described: in `browserFind` (`handlers.go:1020-1027`), check that the
lookup returned an element before writing the ref, and return the same
`element not found: <locator>` error the semantic locators already produce. Making the
CSS path match its siblings would fix the plain output, the `--json` `ok` field, and the
exit code together.

Not proposed here: changing `--all`'s exit code, or the semantic locators' 30s wait —
both are out of scope and behave sensibly today.

## Scope

- **Verified:** macOS darwin 25.5.0, vibium v26.5.31 (installed = npm `latest`,
  published 2026-06-01), two sites — `example.com` and `coffee-cart.app`, identical
  behaviour on both.
- **Not verified:** Linux, Windows. Nothing about the path looks platform-specific, but
  it has not been checked.
- **Checked against `main` — and that is how the fix was found.** Reading
  `internal/agent/handlers.go` on `main` (rather than grepping commit messages) showed
  the CSS path now polls and errors, with a comment describing this exact bug in the
  past tense. See the banner.
- **Workaround verified** (2026-08-07): `count` returns `0` on a miss and `1` on a hit,
  so the guard below behaves correctly.

## Hardening log

- **2026-08-07** — found by following up the `#212` closing comment. Reproduced on
  `example.com`; cross-checked on `coffee-cart.app` (identical). Matrix run across every
  `find` variant, `--all`, `--json` and `count`, establishing that only the CSS path is
  affected. Ref-overwrite behaviour confirmed. Re-verified from a clean daemon after an
  earlier run left the daemon inconsistent following several 30s locator timeouts.
- **2026-08-07, fact-check of this write-up** — three corrections, one fatal:
  1. **Fatal:** "unfiled and unfixed" was false. Fixed on `main` 2026-08-03. Found by
     reading the source, *after* a keyword commit-grep had said otherwise.
  2. A "clean-daemon re-verification" appeared to show the semantic locators *also*
     minting refs, which would have inverted the central argument. **That result was a
     test-harness bug, not a vibium behaviour**: zsh does not word-split unquoted
     variables, so `$V find $loc` sent `"text NoSuchTextZZZ"` as a *single CSS selector*.
     Proven with `set -- $loc; echo $#` → `1`. Re-run with explicit args, the semantic
     locators error correctly, and **the original table was right all along.** Nearly
     "corrected" a correct write-up into a wrong one.
  3. Upgraded `testid`/`xpath`/`role` from "not claimed" to verified, and confirmed the
     `--json` behaviour and the `count` workaround on `coffee-cart.app` too, which the
     "identical behaviour on both sites" claim had been resting on partial evidence.

## Methodology note — keyword-scanning commit messages has now failed four times

Every false "this is unfixed" in this session came from grepping commit *titles*:
`#221` (fix titled after a "shared script result decoder"), `#119` (titled "Write
screenshots where the CLI was told to"), `#240`/`#158`, and this one (titled "Make `is`
exit-honest and `find --timeout` real"). None contained the keywords being searched.
**Read the relevant source on `main` and check the issue's `state_reason`. Treat a
commit-message grep as capable of proving presence, never absence.**

## Test entry

Add to the suite as B37: assert `vibium find "<guaranteed-miss>"` exits non-zero and does
not emit a `@ref`; assert `--json` reports `ok:false`. Expected to FAIL on v26.5.31.
